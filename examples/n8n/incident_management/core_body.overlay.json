{
  "_meta": {
    "purpose": "F-WF-05 wave seam CLOSED. The canonical content/playbooks/incident_management/playbook.cacao.json now carries the core_body bindings directly, as the single source of truth for all three compile targets, so this overlay is empty and the mirror is byte-identical to the canonical. Kept rather than deleted because apply_overlay.py's module docstring commits to the scripts remaining in place across the seam closure so the build path stays stable; the earlier _meta text said 'deleted', which contradicted that, and collapsing satisfies both.",
    "closed_by": "Canonical promotion: the four bindings previously layered here (classification.classify_significance, regulator_submission.resolve_destination at the early-warning and final-report stages, stage_clock.verdict_for_submission) were promoted verbatim, and timeline_binding.open_timeline / close_timeline were bound alongside them."
  },
  "workflow_overlays": {}
}
