# evidence/ — placeholder

SKELETON-FANOUT placeholder. The CORE-FANOUT-LG sibling that follows
this SKELETON will drop one representative interaction-evidence
artifact here, shaped against
[`schemas/evidence/incidents.schema.json`](../../../../schemas/evidence/incidents.schema.json)
(reused from F-CP-02), produced by the LangGraph adapter for the
incidents evidence stream at execution time.

At the SKELETON layer no compiler emitter is bound and no per-execution
artefact is materialised — see the parent `README.md` and the canonical
playbook under
[`content/playbooks/it_security_support_agent/`](../../../../content/playbooks/it_security_support_agent/)
for the topology this example will eventually emit.
