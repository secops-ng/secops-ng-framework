# Outbound playbook-mappings overlay for supply_chain_security
# (SKELETON placeholder — schema-valid stub only).
#
# This SKELETON card pins the workflow topology and the per-step
# x_secops_ng joins on playbook.cacao.json. The full cross-reference
# graph (OSCAL controls, MITRE D3FEND defensive techniques, OCSF
# telemetry classes, NIS2 / DORA / CRA regulatory backlinks) is the
# explicit out-of-scope surface for the SKELETON layer and is
# delegated to the CORE / EXTEND sibling cards. Per the schema's
# `additionalProperties: false`, all six structural keys must be
# present even when empty; this file is the minimal schema-valid
# placeholder that lets the G-02 finalized-playbook coverage guard
# (tests/content/test_playbook_mapping_coverage.py) accept the new
# SKELETON directory without trapping it as an orphan.
#
# Inbound regulatory anchors under content/mappings/{nis2,dora,cra}/
# are deliberately NOT mutated by this SKELETON card. The NIS2
# Article 21(2)(d) inbound entry at
# content/mappings/nis2/article-21-2-d.yaml already exists and
# currently backlinks playbook.threat_intel_ingest@v1 and
# playbook.contractual_obligations_tracker@v1. Adding
# playbook.supply_chain_security@v1 to that list, alongside the
# matching outbound `nis2:` entry below, is work for the EXTEND
# sibling card so the SKELETON stays a single-deliverable scope.
# The G-02 nis2 orphan-CI lane tolerates net-new playbooks under
# the 7-day grace window keyed on the CACAO finalization marker
# mtime, so the EXTEND sibling has a defined window to land the
# inbound + outbound regulatory closure together.
#
# Schema: schemas/playbook-mappings.schema.json
playbook: playbook.supply_chain_security@v1

oscal: []

d3fend: []

ocsf: []

nis2: []

dora: []

cra: []
