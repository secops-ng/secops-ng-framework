{
  "type": "playbook",
  "spec_version": "2.0",
  "id": "playbook--01a17a07-0000-4000-8000-000000000001",
  "name": "Codebase vulnerability management",
  "description": "SBOM-driven codebase vulnerability management playbook for operators that build or distribute software under NIS2 Art. 21(2)(e) and CRA Annex I §2. Ingest a freshly produced or refreshed SBOM, walk its declared top-level dependencies against a vulnerability database using the operator's locally-runnable scanner CLI (default installable from an EU-hosted package index — no hosted scanner SaaS), score each finding against the operator's coordinated-vulnerability-disclosure (CVD) policy, and emit one disclosure-timeline record per finding for the downstream metrics streams. CORE: the four action bodies bind to deterministic primitives in content.playbooks.codebase_vuln_management.primitives; the per-target byte-parity goldens land alongside the worked examples.",
  "playbook_types": [
    "investigation",
    "notification"
  ],
  "created_by": "identity--c0c0c0c0-c0c0-4c0c-8c0c-c0c0c0c0c0c0",
  "created": "2026-06-18T00:00:00Z",
  "modified": "2026-06-19T00:00:00Z",
  "valid_from": "2026-06-18T00:00:00Z",
  "labels": [
    "nis2",
    "cra",
    "sbom",
    "codebase",
    "vuln-disclosure"
  ],
  "playbook_variables": {
    "__sbom_ref__": {
      "type": "string",
      "description": "Pointer to the canonical SBOM artefact for the release under review. CycloneDX or SPDX, machine-readable. Populated by the build chain upstream of this workflow.",
      "external": true
    },
    "__sbom_bytes__": {
      "type": "string",
      "description": "Raw SBOM artefact bytes, fetched by an operator-side upstream node from __sbom_ref__. Bound to the primitive's `sbom_bytes` argument so the SHA-256 pin is computed against the exact bytes fetched. The fetcher is operator-side (n8n HTTP Request / Temporal activity / LangGraph node) so the workflow stays sovereign-stack neutral.",
      "external": true
    },
    "__sbom_format__": {
      "type": "string",
      "description": "Which SBOM format the artefact at __sbom_ref__ uses. One of: cyclonedx_json, cyclonedx_xml, spdx_json, spdx_tag_value.",
      "external": true
    },
    "__sbom_content_hash__": {
      "type": "string",
      "description": "SHA-256 hex digest of the SBOM artefact bytes as fetched. Pinned at ingest so re-runs against a moved artefact are detectable.",
      "external": false
    },
    "__raw_findings__": {
      "type": "string",
      "description": "Per-finding output emitted by the operator's locally-runnable scanner CLI (one entry per matched (component, version, advisory) triple). Marshalled to the workflow as a JSON-native list; the normalise-findings primitive canonicalises it against the disclosure-timeline contract.",
      "external": true
    },
    "__findings_ref__": {
      "type": "string",
      "description": "Pointer to the normalised per-finding result set produced by review-deps. One entry per (component, version, advisory) triple matched against the SBOM, sorted on (advisory_id, purl, version) so two replays of the same scan collapse to byte-identical bytes.",
      "external": false
    },
    "__finding__": {
      "type": "string",
      "description": "One entry from __findings_ref__ — the per-finding view assess-disclosure and track-timeline iterate against. Operators wire the per-finding loop in their compile target's native idiom (n8n SplitInBatches, Temporal child workflow, LangGraph map); the CORE primitive call is per-finding so the byte-parity guarantee holds.",
      "external": false
    },
    "__finding_severity__": {
      "type": "string",
      "description": "Severity band of __finding__ (mirror of __finding__.severity), surfaced as a flat playbook variable so the assess-disclosure primitive binding stays a flat token list per the F-WF-01 CORE convention.",
      "external": false
    },
    "__awareness_at__": {
      "type": "string",
      "description": "ISO-8601 UTC second-precision timestamp marking when the operator became aware of the finding. Anchors the disclosure-window deadlines; supplied by the upstream intake-sbom step or by the build chain.",
      "external": true
    },
    "__cvd_policy__": {
      "type": "string",
      "description": "Operator's coordinated-vulnerability-disclosure policy as a JSON-native object — `policy_ref` + per-severity hour-offset windows. Bound to the resolve-disclosure-window primitive so the policy revision is pinned at emission time.",
      "external": true
    },
    "__disclosure_window__": {
      "type": "string",
      "description": "Per-finding disclosure-window deadlines produced by assess-disclosure (policy_ref + acknowledge_by / fix_by / disclose_by). Consumed by track-timeline.",
      "external": false
    },
    "__captured_at__": {
      "type": "string",
      "description": "ISO-8601 UTC second-precision timestamp pinned by assess-disclosure at emission. Carried on every per-finding disclosure-timeline record so downstream on-time / breach computations are reproducible.",
      "external": false
    },
    "__ref_viz__": {
      "type": "string",
      "description": "Stable visualisation pointer the per-finding record carries for downstream dashboards. Convention: `viz.<slug>@v<semver>`.",
      "external": true
    },
    "__source_data__": {
      "type": "string",
      "description": "Public-bar-safe shape pointer for the underlying advisory payload (kind + optional ocsf_class_uid / telemetry_ref). The raw advisory payload is deliberately not embedded.",
      "external": true
    },
    "__disclosure_timeline_ref__": {
      "type": "string",
      "description": "Pointer to the disclosure-timeline record set produced by track-timeline. One record per finding, shaped against content/evidence/codebase_vuln_management/disclosure-timeline-record.schema.json.",
      "external": false
    },
    "__disclosure_timeline_record__": {
      "type": "string",
      "description": "Single disclosure-timeline record stub emitted by track-timeline against one __finding__ + __disclosure_window__. Operators aggregate the per-finding records into __disclosure_timeline_ref__ in their compile target's native idiom.",
      "external": false
    }
  },
  "workflow_start": "start--01a17a07-0000-4000-8000-000000000001",
  "workflow": {
    "start--01a17a07-0000-4000-8000-000000000001": {
      "type": "start",
      "name": "codebase_vuln_management_start",
      "on_completion": "action--01a17a07-0000-4000-8000-000000000002"
    },
    "action--01a17a07-0000-4000-8000-000000000002": {
      "type": "action",
      "name": "ingest-sbom",
      "description": "Ingest the canonical SBOM artefact for the release under review, pin its content hash on the case, and stamp the workflow case for downstream evidence joins. Anchors CRA Annex I §2(1) SBOM production.",
      "on_completion": "action--01a17a07-0000-4000-8000-000000000003",
      "in_args": [
        "__sbom_bytes__",
        "__sbom_format__"
      ],
      "out_args": [
        "__sbom_content_hash__"
      ],
      "x_secops_ng": {
        "control_refs": [
          "control.sbom_capture@v1"
        ],
        "metric_refs": [
          "kri.releases_without_sbom@v1"
        ],
        "core_body": {
          "primitive": "content.playbooks.codebase_vuln_management.primitives.sbom.pin_sbom_content_hash",
          "in": {
            "sbom_bytes": "__sbom_bytes__",
            "sbom_format": "__sbom_format__"
          },
          "out": "__sbom_content_hash__"
        }
      }
    },
    "action--01a17a07-0000-4000-8000-000000000003": {
      "type": "action",
      "name": "review-deps",
      "description": "Walk the SBOM's top-level dependencies against a vulnerability database (NVD, OSV, GHSA) using the operator's locally-runnable scanner CLI. Default scanner is installable from an EU-hosted package index; no hosted scanner SaaS dependency. The scanner emits __raw_findings__; the primitive canonicalises it to the playbook contract (one entry per (component, version, advisory) triple, sorted for byte-stability).",
      "on_completion": "action--01a17a07-0000-4000-8000-000000000004",
      "in_args": [
        "__raw_findings__",
        "__sbom_content_hash__"
      ],
      "out_args": [
        "__findings_ref__"
      ],
      "x_secops_ng": {
        "control_refs": [
          "control.sbom_capture@v1"
        ],
        "telemetry_refs": [
          "telemetry.ocsf.vulnerability_finding@v1"
        ],
        "core_body": {
          "primitive": "content.playbooks.codebase_vuln_management.primitives.sbom.normalise_findings",
          "in": {
            "raw_findings": "__raw_findings__",
            "sbom_content_hash": "__sbom_content_hash__"
          },
          "out": "__findings_ref__"
        }
      }
    },
    "action--01a17a07-0000-4000-8000-000000000004": {
      "type": "action",
      "name": "assess-disclosure",
      "description": "Resolve the per-finding disclosure-window deadlines from the operator's coordinated-vulnerability-disclosure (CVD) policy and the severity tier the scanner produced. Per-finding contract: the CORE primitive call computes one window from one (severity, awareness_at, cvd_policy) input; the operator's compile target loops the call over __findings_ref__ in its native idiom. The CRA Article 14 actively-exploited / severe-incident reporting trigger from playbook.vuln_intake@v1 is intentionally NOT duplicated here — that decision is taken upstream against the inbound disclosure feed; this workflow only emits the proactive codebase-side timeline.",
      "on_completion": "action--01a17a07-0000-4000-8000-000000000005",
      "in_args": [
        "__finding_severity__",
        "__awareness_at__",
        "__cvd_policy__"
      ],
      "out_args": [
        "__disclosure_window__"
      ],
      "x_secops_ng": {
        "control_refs": [
          "control.vuln_disclosure_intake@v1"
        ],
        "telemetry_refs": [
          "telemetry.ocsf.vulnerability_finding@v1"
        ],
        "metric_refs": [
          "kpi.vuln_disclosure_sla@v1"
        ],
        "core_body": {
          "primitive": "content.playbooks.codebase_vuln_management.primitives.disclosure_window.resolve_disclosure_window",
          "in": {
            "severity": "__finding_severity__",
            "awareness_at": "__awareness_at__",
            "cvd_policy": "__cvd_policy__"
          },
          "out": "__disclosure_window__"
        }
      }
    },
    "action--01a17a07-0000-4000-8000-000000000005": {
      "type": "action",
      "name": "track-timeline",
      "description": "Emit one disclosure-timeline record per finding, shaped against content/evidence/codebase_vuln_management/disclosure-timeline-record.schema.json. The CORE primitive call builds one record from one (finding, disclosure_window) pair; operators aggregate the per-finding records into __disclosure_timeline_ref__ in their compile target's native idiom. Records carry the advisory id, the affected component and version pinned against the SBOM hash, the severity tier, the disclosure-window deadlines, the public-bar-safe source_data shape pointer, and the ref_viz hook so downstream streams and dashboards can consume them off a single typed channel. The full durable evidence-emitter wiring is owned by the TMP sibling slice.",
      "on_completion": "end--01a17a07-0000-4000-8000-000000000006",
      "in_args": [
        "__finding__",
        "__disclosure_window__",
        "__captured_at__",
        "__ref_viz__",
        "__source_data__"
      ],
      "out_args": [
        "__disclosure_timeline_record__"
      ],
      "x_secops_ng": {
        "control_refs": [
          "control.vuln_disclosure_intake@v1"
        ],
        "telemetry_refs": [
          "telemetry.ocsf.vulnerability_finding@v1"
        ],
        "metric_refs": [
          "kpi.vuln_disclosure_sla@v1",
          "kri.cvd_intake_aging@v1"
        ],
        "core_body": {
          "primitive": "content.playbooks.codebase_vuln_management.primitives.timeline.build_disclosure_timeline_stub",
          "in": {
            "finding": "__finding__",
            "disclosure_window": "__disclosure_window__",
            "captured_at": "__captured_at__",
            "ref_viz": "__ref_viz__",
            "source_data": "__source_data__"
          },
          "out": "__disclosure_timeline_record__"
        }
      }
    },
    "end--01a17a07-0000-4000-8000-000000000006": {
      "type": "end",
      "name": "codebase_vuln_management_end"
    }
  },
  "x_secops_ng": {
    "stable_id": "playbook.codebase_vuln_management@v1",
    "content_version": "0.2.0",
    "maturity": "experimental",
    "compile_targets": ["n8n"],
    "control_refs": [
      "control.sbom_capture@v1",
      "control.vuln_disclosure_intake@v1"
    ],
    "telemetry_refs": [
      "telemetry.ocsf.vulnerability_finding@v1"
    ],
    "metric_refs": [
      "kpi.vuln_disclosure_sla@v1",
      "kri.cvd_intake_aging@v1",
      "kri.releases_without_sbom@v1"
    ],
    "sources": [
      "nis2:art-21-2-e-codebase",
      "cra:annex-i-2-codebase-vuln-mgmt",
      "evidence-stream:vulns"
    ]
  }
}
