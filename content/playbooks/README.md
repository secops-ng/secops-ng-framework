# content/playbooks/

CACAO v2 response playbooks — the portable content this framework exists to
ship. **One directory per playbook, named by its slug.**

## The layout is flat on purpose

A playbook's directory name is its slug, and the slug is its permanent
identity: `x_secops_ng.stable_id` is always `playbook.<slug>@v1`, and that
handle is what compilers, tests, cross-playbook handoffs and downstream
deployments reference. Renaming or moving a playbook breaks real consumers,
so the filesystem carries exactly one stable name per playbook and nothing
else.

There is no category nesting (`incident/`, `compliance/`, `vuln/`) because
most playbooks belong to several categories at once — ransomware containment
is incident response *and* malware handling *and* backup recovery.
Classification lives in metadata, where an artifact can carry many tags:
`playbook_types` and `labels` inside the CACAO document, and the regulatory
anchors in each playbook's `mappings.yaml`. Tooling discovers content by
globbing this directory, not by knowing a taxonomy.

The families are still visible in the names themselves: lifecycle chains
(`alert_triage` → `incident_management` → `post_incident_review`;
`vuln_intake` → `vulnerability_management` → `patch_management`),
regulation-anchored playbooks prefixed by the law they serve (`dora_*`,
`nis2_*`, `cra_*`, `eu_ai_act_*`, `eidas2_*`), identity
(`iam_auditor`, `identity_compromise`, `onboarding_offboarding_tracker`),
scenario response (`phishing_triage`, `ransomware_containment`,
`ddos_response`, `data_exfil`), and posture / programme playbooks.

## What a playbook directory contains

| Entry | Purpose |
|---|---|
| `playbook.cacao.json` | the canonical artifact (CACAO v2 + `x_secops_ng` superset). Some playbooks keep the canonical as `playbook.cacao.yaml` — same schema, YAML serialization. |
| `README.md` | scenario, prerequisites, regulatory anchors, and the bindings an operator supplies |
| `mappings.yaml` | OSCAL / D3FEND / OCSF / KPI wiring into `../mappings/`, `../controls/`, `../metrics/` |
| `primitives/` | where CORE work has landed: deterministic, replay-safe Python bodies that `core_body` bindings reference. Importable as `content.playbooks.<slug>.primitives` — which is why this directory tree is a Python package. |
| `payloads/` | (some playbooks) typed payload models for the shapes the playbook ingests |

Playbooks grow in stages — SKELETON (workflow graph + mappings + README, the
committed contract) → CORE (primitives written and bound, three-target
compiled examples with byte-parity goldens) → EXTEND (cookbook walkthrough,
metric pairs). An action step without a `{primitive, in, out}` binding is an
honest TODO, and the tooling counts it rather than hiding it.

## Entries that are not playbooks

- `_template/` — the contributor scaffold for a new playbook. The underscore
  prefix means "not content"; tooling skips `_*` directories.
- `__init__.py`, `__pycache__/` — package plumbing and build residue of the
  importable primitives. Not content.
- `alert_triage.cacao.yaml` — the one layout exception: a directory-level
  YAML canonical whose semantically identical JSON twin lives at
  `alert_triage/playbook.cacao.json`. Edit both or neither;
  `tests/content/test_playbook_mirror_parity.py` guards the pair against
  drift.

## Catalogue by family

The table below is **generated** — hand-maintained tables in this file
drifted every time one existed (an earlier revision listed six playbooks
while the tree held forty-seven). `tools/render_playbook_table.py` renders it
from the CACAO sources; a test under `tests/content/` fails whenever the
committed block no longer matches a fresh render, and a new playbook must be
filed into a family there before the suite passes. Descriptions are the first
sentence of each playbook's own `description`, verbatim.

<!-- playbook-table:begin — generated by tools/render_playbook_table.py; do not hand-edit -->

_48 playbooks. Regenerate with `python -m tools.render_playbook_table`; CI fails when this block is stale._

### Alert & incident lifecycle

| Playbook | What it does |
|---|---|
| [`alert_triage`](alert_triage/) | SOC alert_triage source playbook |
| [`incident_management`](incident_management/) | Manage a significant security incident through the NIS2 Article 23 three-stage regulator timeline: intake the originating signal, classif… |
| [`post_incident_review`](post_incident_review/) | Run the post-incident review after an incident has been closed or contained: collate the timeline from the artifacts the responders left… |

### Scenario response

| Playbook | What it does |
|---|---|
| [`agentic_threat_response`](agentic_threat_response/) | CACAO v2 scaffold for detecting and initially responding to fully-agentic adversary activity (autonomous LLM-driven credential harvest, l… |
| [`cloud_misconfiguration`](cloud_misconfiguration/) | Respond to a cloud-posture (CSPM) finding that flags a sensitive misconfiguration: public storage exposure, over-permissive identity, mis… |
| [`data_exfil`](data_exfil/) | Respond to a DLP / egress signal that indicates possible exfiltration of sensitive data |
| [`ddos_response`](ddos_response/) | Operationalise the incident-handling capability for the availability/denial-of-service attack dimension: detect an availability anomaly o… |
| [`identity_compromise`](identity_compromise/) | Respond to a detected account compromise (credential theft, MFA bypass, anomalous sign-in, suspicious OAuth grant, or impossible-travel s… |
| [`phishing_triage`](phishing_triage/) | Inbound suspicious-email triage |
| [`ransomware_containment`](ransomware_containment/) | Contain an in-progress or just-detected ransomware event on an endpoint or identity |

### Vulnerability lifecycle

| Playbook | What it does |
|---|---|
| [`codebase_vuln_management`](codebase_vuln_management/) | SBOM-driven codebase vulnerability management playbook for operators that build or distribute software under NIS2 Art. 21(2)(e) and CRA A… |
| [`patch_management`](patch_management/) | Operationalise the patch / update maintenance capability against the operator's own deployed estate: detect that a security update is ava… |
| [`vuln_intake`](vuln_intake/) | Coordinated vulnerability disclosure (CVD) intake playbook for CRA-aligned operators |
| [`vulnerability_management`](vulnerability_management/) | CACAO v2 scaffold for the operator-side vulnerability and patch management lifecycle a regulated operator runs against its own deployed e… |

### Threat intel & detection engineering

| Playbook | What it does |
|---|---|
| [`detection_engineering`](detection_engineering/) | Source playbook for the detection_engineering rule lifecycle |
| [`threat_intel_ingest`](threat_intel_ingest/) | Portable response for ingesting external cyber threat intelligence |

### Identity & access

| Playbook | What it does |
|---|---|
| [`iam_auditor`](iam_auditor/) | Per-execution capability-inventory workflow |
| [`mfa_secured_comms`](mfa_secured_comms/) | Operate the multi-factor / continuous-authentication and secured-communications posture surface required by NIS2 Art.21(2)(j) and DORA Ar… |
| [`onboarding_offboarding_tracker`](onboarding_offboarding_tracker/) | Identity-lifecycle grant/revoke-confirmation workflow under NIS2 Article 21(2)(i) — human-resources security, access-control policies, an… |

### Posture & asset management

| Playbook | What it does |
|---|---|
| [`asset_management`](asset_management/) | Operationalise the asset and configuration management capability against the operator's own deployed estate: ingest the documented asset-… |
| [`crypto_posture_management`](crypto_posture_management/) | Operate the cryptography & encryption posture surface required by NIS2 Art.21(2)(h): inventory the operator's declared cryptography polic… |
| [`cryptographic_controls`](cryptographic_controls/) | Operator-side lifecycle of the cryptographic-controls surface an essential or important entity operates against its documented cryptograp… |
| [`infra_posture_management`](infra_posture_management/) | Continuous infrastructure-posture-management workflow |
| [`network_security`](network_security/) | CACAO v2 playbook operationalising the per-window reconciliation of a declared segmentation policy against the observed network-boundary… |

### Supply chain & contracts

| Playbook | What it does |
|---|---|
| [`contractual_obligations_tracker`](contractual_obligations_tracker/) | Supplier-contract obligation tracking workflow under NIS2 Article 21(2)(d) supply-chain security |
| [`supply_chain_security`](supply_chain_security/) | Supply-chain-security playbook (NIS2 Directive (EU) 2022/2555, Article 21(2)(d)) |

### Resilience & continuity

| Playbook | What it does |
|---|---|
| [`backup_recovery`](backup_recovery/) | Exercise the business-continuity surface: on a scheduled or operator-triggered restore-drill window, validate the integrity of the most r… |
| [`business_continuity`](business_continuity/) | CACAO v2 scaffold for the operator-side business continuity lifecycle a NIS2 essential or important entity runs when a business-continuit… |

### Privacy & data protection

| Playbook | What it does |
|---|---|
| [`data_protection_impact_assessment`](data_protection_impact_assessment/) | CACAO v2 scaffold for the operator-side data protection impact assessment (DPIA) lifecycle a controller runs before deploying processing… |
| [`data_subject_rights`](data_subject_rights/) | CACAO v2 scaffold for the operator-side data subject rights (DSR) intake and fulfilment lifecycle a controller runs when a data subject e… |

### SOC operations & programme

| Playbook | What it does |
|---|---|
| [`ai_human_oversight`](ai_human_oversight/) | CACAO v2 scaffold for the deployer-side EU AI Act (Regulation (EU) 2024/1689) Article 14 human-oversight loop: the cycle an oversight fun… |
| [`cyber_hygiene_training`](cyber_hygiene_training/) | Operate the basic cyber-hygiene and staff cybersecurity-training posture surface required by NIS2 Art.21(2)(g): inventory the in-scope tr… |
| [`executive_metrics`](executive_metrics/) | Recurring rollup of the operator's KPI/KRI catalog into a board-ready summary plus a control-effectiveness score |
| [`it_security_support_agent`](it_security_support_agent/) | Ticket-shaped interaction workflow that ingests one operator-supplied support request, classifies it (informational / actionable / incide… |
| [`on_call_rotation`](on_call_rotation/) | Operate the on-call rotation: load the current rotation roster, bind the escalation tiers (primary / secondary / manager) the operator's… |
| [`security_awareness_training`](security_awareness_training/) | SKELETON scaffold for the structured security-awareness training programme lifecycle required by NIS2 Art. 21(2)(g) |

### Regulatory programmes

| Playbook | What it does |
|---|---|
| [`cra_cvd`](cra_cvd/) | CACAO v2 scaffold for the operator-side coordinated vulnerability disclosure (CVD) lifecycle a manufacturer of a product with digital ele… |
| [`cra_srp_notify`](cra_srp_notify/) | durable-state scaffold for the CRA Article 14 notification workflow that will address the Single Reporting Platform (SRP) once its schema… |
| [`dora_ict_risk_selfassess`](dora_ict_risk_selfassess/) | CACAO v2 scaffold for the DORA Chapter II ICT risk management framework operator-side self-assessment report |
| [`dora_major_incident_reporting`](dora_major_incident_reporting/) | CACAO v2 scaffold for the DORA Chapter III major-ICT-related incident reporting lifecycle a financial entity discharges to its competent… |
| [`dora_tlpt_programme`](dora_tlpt_programme/) | CACAO v2 scaffold for the DORA Chapter IV digital operational resilience testing (DORT) programme a financial entity operates against its… |
| [`dora_tpr_management`](dora_tpr_management/) | CACAO v2 scaffold for the DORA Chapter V ICT third-party risk management workflow an EU financial entity operates against every ICT third… |
| [`eidas2_identity_verification`](eidas2_identity_verification/) | CACAO v2 scaffold for the operator-side EU Digital Identity Wallet (EUDIW) identity-verification lifecycle a regulated operator runs when… |
| [`eu_ai_act_deployer_obligations`](eu_ai_act_deployer_obligations/) | CACAO v2 playbook for the operator-side EU AI Act (Regulation (EU) 2024/1689) Article 26 deployer-obligation lifecycle, gated by the Arti… |
| [`eu_ai_act_risk_management`](eu_ai_act_risk_management/) | CACAO v2 scaffold for the risk-management system Article 9 of the EU AI Act (Regulation (EU) 2024/1689) requires providers of high-risk A… |
| [`nis2_art20_governance`](nis2_art20_governance/) | CACAO v2 scaffold for the operator-side NIS2 Directive (EU) 2022/2555, Article 20 management-body cybersecurity governance lifecycle |
| [`nis2_self_assessment`](nis2_self_assessment/) | CACAO v2 scaffold for the NIS2 Article 21(2) operator-side self-assessment report |
| [`soc2_evidence_collector`](soc2_evidence_collector/) | Aggregates the evidence the operator's other playbooks already emit into a dated SOC 2 readiness attestation |

<!-- playbook-table:end -->

## Counting and cataloguing

Do not hand-count this directory — a naive `content/playbooks/*/` glob
counts `_template` and `__pycache__` too. For per-playbook *readiness*
(bindings present, remaining work, which sources cannot compile), the
catalogue script computes the truth:

```sh
python .claude/skills/compile-playbooks/scripts/catalog.py --table
```

To choose a playbook and a compile target, start at
[`docs/quickstart/README.md`](../../docs/quickstart/README.md).
