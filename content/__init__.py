"""Content layer namespace package.

Holds portable artifacts (CACAO playbooks, OSCAL/D3FEND mappings, OCSF data
shapes, KPI/KRI catalogue). The shipped layout uses hyphenated directory
names (e.g. ``vuln-intake``); per-workflow primitives are re-exported under
underscore-aliased import paths from this package so Python imports remain
PEP-8 friendly.
"""
