{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://secops-ng.org/schemas/content/metrics/_schema/metric.schema.json",
  "title": "SecOps-NG catalog metric entry (pointer)",
  "description": "Thin pointer schema for catalog entries under content/metrics/. The canonical schema is content-model/metrics.schema.json; this file re-exports it via $ref so authors and validators inside content/metrics/ can resolve the schema with a local relative path. Do not diverge this file from the canonical schema — update the canonical schema and re-export here.",
  "$ref": "../../../content-model/metrics.schema.json"
}
