# <workflow-name> — example configuration
#
# Every documented option this workflow exposes appears here with its
# default value and a short comment. Operators copy this file, edit
# it, and load it through `secops_ng.config` (or equivalent) at
# workflow startup.
#
# Shape:
#   key: value  # description, type, default

# --- Language model ---------------------------------------------------
# model: openai/gpt-4o-mini   # DSPy LM identifier, str, default "openai/gpt-4o-mini"
# api_key_env: OPENAI_API_KEY # env var name holding the LM API key, str, default "OPENAI_API_KEY"

# --- Workflow knobs ---------------------------------------------------
# TODO: replace with the actual config keys this workflow accepts.
