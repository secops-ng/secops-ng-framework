"""End-user cookbook example: run the <workflow-name> workflow.

Replace this stub with a runnable invocation of the graph re-exported
from ``primitives.py``. The example should be self-contained, safe to
run offline (it may fail at the LM step without credentials — that is
intentional), and short enough to read in one sitting.

Run with::

    python workflows/<workflow-name>/example.py
"""

from __future__ import annotations


def main() -> None:
    # TODO: import the state type and compiled graph from primitives,
    # construct an initial state, invoke the graph, and print the
    # interesting fields from the final state.
    raise NotImplementedError("Replace this stub when copying the template.")


if __name__ == "__main__":
    main()
