{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://secops-ng.org/schemas/mapping.schema.json",
  "title": "SecOps-NG Regulatory Mapping Document",
  "description": "Machine-readable crosswalk from regulatory obligations to SecOps-NG content-model artifact IDs (controls, playbooks, metrics, detections, telemetry). Files live under content/mappings/<regime>/ and group obligations by article: each file holds an `entries` list, one object per obligation atom. Mappings are structural pointers, not legal interpretation: they tell a reviewer 'this regulatory clause is exercised by these artifacts', and they let a compiler regenerate evidence packages from a regime selection.",
  "type": "object",
  "additionalProperties": false,
  "required": ["regime", "entries"],
  "properties": {
    "regime": {
      "description": "Regime key matching the directory name under content/mappings/.",
      "type": "string",
      "enum": ["nis2", "dora", "cra", "gdpr", "eu_ai_act", "iso27001", "soc2"]
    },
    "entries": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/entry" }
    }
  },
  "$defs": {
    "entry": {
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "regulation",
    "obligation",
    "status"
  ],
  "properties": {
    "id": {
      "description": "Stable mapping identifier. Format: `<regime>:<slug>` where <regime> is the lowercase regime key (nis2, dora, cra, gdpr, iso27001, soc2) and <slug> is a short kebab-case obligation slug, typically derived from the article/section number plus a subpoint.",
      "type": "string",
      "pattern": "^(nis2|dora|cra|gdpr|eu_ai_act|iso27001|soc2):[a-z0-9][a-z0-9.-]*$",
      "minLength": 3,
      "maxLength": 120
    },
    "regulation": {
      "description": "Citation block. Identifies the source instrument and the specific article/section the obligation comes from. Public-bar text only: EUR-Lex CELEX numbers and OJ references are fine, national transposition acts must be cited by name only (no commentary).",
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "instrument", "article"],
      "properties": {
        "name": {
          "description": "Short name (NIS2, DORA, CRA, GDPR, ISO 27001, SOC 2).",
          "type": "string",
          "minLength": 1,
          "maxLength": 40
        },
        "instrument": {
          "description": "Full instrument identifier, e.g. 'Directive (EU) 2022/2555' or 'Regulation (EU) 2024/2847'.",
          "type": "string",
          "minLength": 1,
          "maxLength": 200
        },
        "celex": {
          "description": "EUR-Lex CELEX number when applicable.",
          "type": "string",
          "pattern": "^[0-9]{5}[A-Z][0-9]{4}(R[0-9]+)?$"
        },
        "article": {
          "description": "Article / section / annex reference verbatim from the instrument, e.g. '21(2)(a)' or 'Annex I §1.b'.",
          "type": "string",
          "minLength": 1,
          "maxLength": 80
        },
        "url": {
          "description": "Stable URL to the cited instrument. Prefer EUR-Lex permalinks.",
          "type": "string",
          "format": "uri"
        }
      }
    },
    "obligation": {
      "description": "Short paraphrase of the obligation atom this entry maps. NOT a quote of regulatory text and NOT legal interpretation; one or two sentences naming what the regulated entity must do, in plain language a reviewer can sanity-check against the cited article.",
      "type": "string",
      "minLength": 1,
      "maxLength": 1000
    },
    "status": {
      "description": "Maturity of the mapping. `draft` = mapping authored, target artifacts may not all exist yet; `provisional` = target artifacts exist as stubs; `live` = every referenced artifact has shipped content.",
      "type": "string",
      "enum": ["draft", "provisional", "live"]
    },
    "control_refs": {
      "description": "Control stable IDs (long-form URN `control.<slug>@v<semver>`) that implement this obligation. Compilers and reviewers join into content-model/control.schema.json instances via these IDs. Short-form `ctl:` refs are rejected — see content-model/README.md § 'Canonical URN scheme'.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^control\\.[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)*@v[0-9]+(\\.[0-9]+){0,2}$",
        "maxLength": 200
      },
      "uniqueItems": true
    },
    "playbook_refs": {
      "description": "CACAO playbook stable IDs (long-form URN `playbook.<slug>@v<semver>`) that exercise this obligation. Joins to content-model/playbook.schema.json#x_secops_ng.stable_id. Short-form `pb:` refs are rejected.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^playbook\\.[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)*@v[0-9]+(\\.[0-9]+){0,2}$",
        "maxLength": 200
      },
      "uniqueItems": true
    },
    "metric_refs": {
      "description": "KPI / KRI catalog IDs (long-form URN `kpi.<slug>@v<semver>` or `kri.<slug>@v<semver>`) that measure performance against this obligation. Short-form `kpi:` / `kri:` refs are rejected.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^(kpi|kri)\\.[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)*@v[0-9]+(\\.[0-9]+){0,2}$",
        "maxLength": 200
      },
      "uniqueItems": true
    },
    "detection_refs": {
      "description": "Sigma rule pointers (long-form URN `detection.<slug>@v<semver>`) that produce signal relevant to this obligation. Rarely populated — most regulatory obligations are evidence/process obligations rather than detection obligations. Short-form `det:` refs are rejected.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^detection\\.[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)*@v[0-9]+(\\.[0-9]+){0,2}$",
        "maxLength": 200
      },
      "uniqueItems": true
    },
    "telemetry_refs": {
      "description": "OCSF telemetry binding IDs (long-form URN `telemetry.<slug>@v<semver>`) that this obligation reads from. Short-form `tlm:` refs are rejected.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^telemetry\\.[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)*@v[0-9]+(\\.[0-9]+){0,2}$",
        "maxLength": 200
      },
      "uniqueItems": true
    },
    "evidence_stream_refs": {
      "description": "Evidence-stream identifiers under content/evidence/<stream>/ that emit artifacts satisfying this obligation. Each entry is the stream slug (e.g. `risk-analysis`). Joins to schemas/evidence/<stream>.schema.json. Short slugs match the on-disk directory under content/evidence/.",
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9-]*$",
        "maxLength": 80
      },
      "uniqueItems": true
    },
    "notes": {
      "description": "Optional reviewer notes — implementation guidance, scope caveats, references to ENISA / ESAs / ENISA technical guidance. Forward-public hygiene applies.",
      "type": "string",
      "maxLength": 4000
    }
  }
    }
  }
}
